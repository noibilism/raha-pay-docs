import { createFileRoute, notFound } from "@tanstack/react-router";
import { DocsShell } from "@/components/docs/docs-shell";
import { GuideContent } from "@/components/docs/doc-content";
import { getGuide } from "@/data/guides";
import { guideMarkdown } from "@/data/markdown";
export const Route=createFileRoute("/guides/$slug")({server:{handlers:{GET:({params})=>{if(!params.slug.endsWith(".md"))return undefined;const guide=getGuide(params.slug.slice(0,-3));return guide?new Response(guideMarkdown(guide),{headers:{"content-type":"text/markdown; charset=utf-8"}}):new Response("Not found",{status:404})}}},loader:({params})=>{const guide=getGuide(params.slug);if(!guide)throw notFound();return guide},head:({loaderData})=>({meta:[{title:loaderData?`${loaderData.title} — Raha Pay Docs`:"Guide unavailable — Raha Pay Docs"},{name:"description",content:loaderData?.description??"The requested Raha Pay guide is unavailable."},{property:"og:title",content:loaderData?`${loaderData.title} — Raha Pay Docs`:"Guide unavailable — Raha Pay Docs"},{property:"og:description",content:loaderData?.description??"The requested Raha Pay guide is unavailable."},{property:"og:type",content:"article"},{name:"twitter:card",content:"summary_large_image"}]}),component:GuidePage});
function GuidePage(){const guide=Route.useLoaderData();return <DocsShell currentPath={`/guides/${guide.slug}`} anchors={guide.sections.map(s=>({id:s.id,title:s.title}))}><GuideContent guide={guide}/></DocsShell>}
