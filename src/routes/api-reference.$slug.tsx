import { createFileRoute, notFound } from "@tanstack/react-router";
import { DocsShell } from "@/components/docs/docs-shell";
import { EndpointCodeRail, EndpointContent } from "@/components/docs/endpoint-content";
import { getEndpoint } from "@/data/endpoints";
export const Route=createFileRoute("/api-reference/$slug")({loader:({params})=>{const endpoint=getEndpoint(params.slug);if(!endpoint)throw notFound();return endpoint},head:({loaderData})=>({meta:[{title:loaderData?`${loaderData.title} — Raha Pay API Reference`:"Endpoint unavailable — Raha Pay Docs"},{name:"description",content:loaderData?.description??"The requested endpoint is unavailable."},{property:"og:title",content:loaderData?`${loaderData.title} — Raha Pay API Reference`:"Endpoint unavailable — Raha Pay Docs"},{property:"og:description",content:loaderData?.description??"The requested endpoint is unavailable."},{property:"og:type",content:"article"},{name:"twitter:card",content:"summary_large_image"}]}),component:EndpointPage});
function EndpointPage(){const endpoint=Route.useLoaderData();return <DocsShell api currentPath={`/api-reference/${endpoint.slug}`} anchors={[]} rightRail={<EndpointCodeRail endpoint={endpoint}/>}><EndpointContent endpoint={endpoint}/><div className="mt-8 xl:hidden"><EndpointCodeRail endpoint={endpoint}/></div></DocsShell>}
