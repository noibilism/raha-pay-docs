import { createFileRoute, Link } from "@tanstack/react-router";
import { ArrowRight } from "lucide-react";
import { DocsShell } from "@/components/docs/docs-shell";
import { endpointGroups, endpoints } from "@/data/endpoints";
import { cn } from "@/lib/utils";
export const Route=createFileRoute("/api-reference")({head:()=>({meta:[{title:"API Reference — Raha Pay Docs"},{name:"description",content:"Complete KES endpoint reference for Raha Pay collections, payouts, accounts, balances, conversions and webhooks."},{property:"og:title",content:"Raha Pay KES API Reference"},{property:"og:description",content:"Methods, request fields, examples, responses and errors for every Raha Pay KES endpoint."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary_large_image"}]}),component:ApiIndex});
function ApiIndex(){return <DocsShell currentPath="/api-reference" api><p className="mb-3 text-xs font-bold uppercase text-primary">API Reference</p><h1 className="font-display text-4xl font-bold sm:text-5xl">KES API endpoints</h1><p className="mt-5 max-w-2xl text-lg leading-8 text-muted-foreground">Build and reconcile every Raha Pay money movement with predictable JSON endpoints.</p><div className="mt-12 space-y-10">{endpointGroups.map(group=><section key={group}><h2 className="font-display text-2xl font-bold">{group}</h2><div className="mt-4 divide-y rounded-md border">{endpoints.filter(e=>e.group===group).map(endpoint=><Link key={endpoint.slug} to="/api-reference/$slug" params={{slug:endpoint.slug}} className="group flex items-center gap-4 p-4 hover:bg-accent"><span className={cn("w-11 font-mono text-xs font-bold",endpoint.method==="GET"?"text-method-get":"text-method-post")}>{endpoint.method}</span><span className="min-w-0 flex-1"><span className="block text-sm font-semibold">{endpoint.title}</span><code className="mt-1 block truncate text-xs text-muted-foreground">{endpoint.path}</code></span><ArrowRight className="size-4 text-muted-foreground group-hover:text-primary"/></Link>)}</div></section>)}</div></DocsShell>}
