import type { DiagramType } from "@/components/docs/sequence-diagram";

export type CalloutKind = "note" | "warning" | "tip";
export type GuideTable = { headers: string[]; rows: string[][]; searchable?: boolean };
export type GuideStep = { id: string; title: string; paragraphs: string[]; outcome?: string; codeKind?: "request" | "response" | "webhook" };
export type GuideSection = { id: string; title: string; paragraphs?: string[]; bullets?: string[]; callout?: { kind: CalloutKind; title: string; text: string; code?: string }; code?: string; diagram?: DiagramType | undefined; table?: GuideTable };
export type Guide = { slug: string; group: string; title: string; description: string; sections: GuideSection[]; steps?: GuideStep[] | undefined; endpointSlug?: string | undefined; diagram?: DiagramType | undefined; statuses?: string[] | undefined; edgeCases?: string[] | undefined; sandboxTests?: string[][] | undefined; quickstart?: boolean };

const guidesData: Array<Omit<Guide, "sections"> & { purpose: string; useWhen: string; build: string; failures: string; testing: string; sections?: GuideSection[] }> = [
  { slug:"introduction", group:"Start here", title:"Welcome", description:"Accept payments, send payouts and reconcile your Raha Pay account.", purpose:"Raha Pay connects your server to M-Pesa, Airtel Money and Kenyan banks through one consistent API. Your application creates a payment or payout, stores the returned Raha Pay ID, and receives the final result through a signed event.", useWhen:"Start here if you are choosing an integration path or learning the shared conventions. Use hosted checkout when you want Raha Pay to present payment choices. Use direct payment endpoints when your product owns the customer interface.", build:"Create requests only from a trusted server. Send amounts as integer cents, add your own unique reference, and include an idempotency key on every create operation. Keep the Raha Pay ID beside your internal order or ledger entry.", failures:"Network acceptance is not a final result. Keep an operation open while it is awaiting a customer or processing at a bank. Update your records from verified events and reconcile against the transactions endpoint.", testing:"Build against Sandbox first. Exercise successful, declined, insufficient-funds and timeout outcomes before switching credentials and webhook secrets." },
  { slug:"quickstart", group:"Start here", title:"Quickstart: your first M-Pesa payment in 5 minutes", description:"Send an STK prompt, receive the event and confirm the payment.", endpointSlug:"create-mobile-payment", diagram:"mobile", statuses:["initiated","awaiting_customer","successful","failed"], quickstart:true, purpose:"This quickstart takes one order from an API key to a confirmed M-Pesa payment. You will create the payment from your server, approve the prompt on a Sandbox number, receive a signed event and fetch the stored payment.", useWhen:"Use this path for a server-rendered checkout or mobile application whose backend controls the order. Choose hosted checkout instead when you do not want to build a payment selection screen.", build:"Use a unique reference that maps to one order. Raha Pay returns an rp_pay_ identifier immediately. Save it before showing the customer a waiting state, because it is the stable key for later retrieval and reconciliation.", failures:"A timeout does not prove the payment failed. The customer may have approved while a network response was delayed. Wait for a final event or retrieve the payment before creating a replacement.", testing:"Use the Sandbox numbers below to force each outcome. Reuse the same idempotency key only when repeating the exact same request after an uncertain network result." },
  { slug:"api-keys-environments", group:"Start here", title:"API keys and environments", description:"Authenticate requests and keep Sandbox separate from Live.", purpose:"Secret keys authenticate server requests. Public keys identify your account in browser-safe flows but cannot create payouts or read private account data. Sandbox and Live have separate credentials, records, webhook secrets and allowlists.", useWhen:"Read this before making your first call or rotating credentials. Use a restricted server key for each deployed service rather than sharing one key across unrelated systems.", build:"Send the secret key as a Bearer token in the Authorization header. Load it from your deployment secret store, never from source control, a browser bundle, a mobile application or a client-visible log.", failures:"A 401 usually means the key is missing, truncated, revoked or sent to the wrong environment. A 403 means the key is valid but the operation or source address is not allowed.", testing:"Keep Sandbox and Live values in separate deployment settings. Test key rotation by activating a second key, updating callers, confirming traffic and only then revoking the old key." },
  { slug:"sandbox-testing", group:"Start here", title:"Sandbox testing", description:"Run repeatable payment and payout tests without moving real funds.", purpose:"Sandbox mirrors the request shape, response envelope, status transitions and webhook signatures used in Live. Deterministic test details let your automated suite cover outcomes that are difficult or unsafe to force with real money.", useWhen:"Use Sandbox throughout development, CI and release verification. Move to Live only after webhook verification, retries, duplicate handling and reconciliation have been tested.", build:"Create Sandbox keys and a Sandbox webhook endpoint in the dashboard. Use the prescribed phone or account values to select an outcome, then assert both the synchronous response and the later event.", failures:"Do not treat a successful create response as a completed movement. Test delayed events, duplicate events and an API timeout after acceptance. Your records should still converge to one final result.", testing:"Reset test records by creating a new merchant reference. Simulated balance top-ups are immediate and affect only Sandbox." },
  { slug:"payments-work", group:"Accept payments", title:"How payments work", description:"Understand the payment lifecycle before building a collection flow.", endpointSlug:"create-mobile-payment", diagram:"mobile", statuses:["initiated","awaiting_customer","processing","successful","failed"], purpose:"A payment records a request to collect money from a customer. The create call starts the flow; M-Pesa, Airtel Money or a bank completes it asynchronously. Your order should follow the payment status rather than the browser session.", useWhen:"Use direct payments when your application controls the amount, customer details and payment rail. Use payment links for invoices and chat-based sales, or hosted checkout when customers should choose a rail on a Raha Pay page.", build:"Create the payment once, save its ID and reference, and show the correct customer action. For STK flows, show a waiting state. For bank transfers, show the exact account details and expiry. Process the signed final event before fulfilment.", failures:"Customers can cancel, enter the wrong PIN, run out of funds or let a prompt expire. Network timeouts can happen after acceptance. Retry only with the original idempotency key until you know the first request did not create a payment.", testing:"Cover every final status and at least one delayed event. Run a daily comparison between your successful payments and the transactions endpoint to catch missed application updates." },
  { slug:"mpesa-payment", group:"Accept payments", title:"M-Pesa STK push", description:"Prompt a Safaricom customer to approve a payment on their phone.", endpointSlug:"create-mobile-payment", diagram:"mobile", statuses:["initiated","awaiting_customer","successful","failed"], purpose:"An STK push opens an M-Pesa approval prompt on a Safaricom phone. The customer checks the business name and amount, enters their PIN, and M-Pesa sends the result back to Raha Pay.", useWhen:"Use STK push when the customer is present and can act on their Safaricom phone. Use a bank transfer or payment link when the payer is remote, uses another network, or needs more time.", build:"Collect the number in local form for display, normalize it to 2547XXXXXXXX on your server, and create one payment. Show an instruction to check the phone. Keep polling light and rely on the signed event for the final result.", failures:"The customer has about 60 seconds to enter their PIN. They can cancel from the phone. Only one STK prompt can be open per number at a time. A timeout is not a failure until Raha Pay returns or emits a final status.", testing:"Use each Sandbox number to verify customer approval, insufficient funds, cancellation and timeout. Confirm that retries do not create a second charge." },
  { slug:"airtel-money-payment", group:"Accept payments", title:"Airtel Money", description:"Prompt an Airtel Money customer to approve a wallet payment.", endpointSlug:"create-mobile-payment", diagram:"airtel", statuses:["initiated","awaiting_customer","successful","failed"], purpose:"An Airtel Money payment asks the customer to approve a wallet debit on their Airtel line. The request and event shapes match M-Pesa, while the network value and customer experience differ.", useWhen:"Use this flow for active Airtel Money customers. Use M-Pesa for Safaricom numbers and hosted checkout when your product should let the customer choose.", build:"Normalize the phone number, set network to airtel_money, create the payment with a unique reference and save the returned ID. Tell the customer to approve on the phone, then wait for a final event.", failures:"A line may be inactive, registered to another wallet state, short of funds or already handling an approval. Do not infer success from the create response or from a browser redirect.", testing:"Exercise successful, insufficient-funds, cancelled and timed-out outcomes. Verify that a duplicate event leaves your order unchanged." },
  { slug:"bank-transfer-payment", group:"Accept payments", title:"Bank transfer", description:"Give a customer account details for a single bank payment.", endpointSlug:"create-bank-payment", diagram:"bank", statuses:["initiated","awaiting_customer","successful","failed","expired"], purpose:"A bank transfer payment returns account details tied to one expected amount and reference. Raha Pay matches the incoming credit and sends the payment result to your webhook.", useWhen:"Use this flow for larger payments, customers who prefer a banking app, or a checkout that may outlive a mobile prompt. Use a dedicated account when one customer needs a reusable account number.", build:"Create the payment, display the bank name, account name, account number, amount and expiry exactly as returned, and keep the order pending. Match the final event to the stored payment ID.", failures:"Customers can transfer after expiry, use another name, or send less or more than requested. Do not fulfil from a bank screenshot. Use the credited amount in the event and apply your amount-mismatch policy.", testing:"Use the Sandbox account values to produce exact, underpaid, overpaid and expired results. Verify that your UI removes expired instructions." },
  { slug:"hosted-checkout", group:"Accept payments", title:"Hosted checkout", description:"Redirect customers to a Raha Pay page to choose a payment method.", endpointSlug:"create-hosted-checkout", diagram:"mobile", statuses:["created","awaiting_customer","successful","failed","expired"], purpose:"Hosted checkout provides a Raha Pay payment page with the enabled rails for your account. It reduces the interface and validation work in your product while your server keeps control of the order.", useWhen:"Use hosted checkout when you want a quick web integration or several payment methods on one screen. Use direct payments when you need a fully embedded experience.", build:"Create checkout on your server, redirect to checkout_url, and provide an HTTPS callback URL. The callback returns the customer to your product; the signed webhook confirms the money movement.", failures:"A browser return can be replayed, skipped or opened before the event reaches you. Never fulfil from query parameters. Retrieve the payment when a returning customer sees a pending state.", testing:"Close the checkout, revisit it, complete it twice and exercise an expired session. Confirm that one merchant reference produces one order update." },
  { slug:"payment-links", group:"Accept payments", title:"Payment links", description:"Create a shareable payment URL for invoices, messages and sales flows.", endpointSlug:"create-payment-link", statuses:["active","successful","expired"], purpose:"A payment link gives you a hosted URL without requiring a checkout session in your application. You can send it by email or message, add it to an invoice, or display it as a button.", useWhen:"Use a link when payment begins outside your product or the customer may return later. Use hosted checkout when your application initiates and tracks an active browser session.", build:"Create the link with a clear name, reference and optional fixed amount. Save its ID, share the returned HTTPS URL and listen for standard payment events created from that link.", failures:"Links can be shared with the wrong person or opened more than once. Tie fulfilment to the payment reference and customer record, not to a page visit.", testing:"Open the link in a private browser, test each enabled rail and confirm that duplicate browser refreshes do not duplicate fulfilment." },
  { slug:"dedicated-accounts", group:"Accept payments", title:"Dedicated account numbers", description:"Assign reusable or one-time bank account details to a customer.", endpointSlug:"create-dedicated-account", diagram:"bank", statuses:["active","expired"], purpose:"A dedicated account number lets Raha Pay identify an incoming bank transfer without asking the payer to copy a payment reference. Reusable accounts map to one customer; one-time accounts map to one expected payment.", useWhen:"Use reusable accounts for wallets, marketplaces and repeat customers. Use one-time accounts for invoices with a fixed amount and expiry. Use an ordinary bank payment when you do not need a persistent mapping.", build:"Create the account with a unique customer reference, store the returned account details, and display them exactly. For one-time accounts, store the amount and expiry. Credit your customer only after a verified dedicated_account.credited event.", failures:"Transfers may arrive from a different sender name, after expiry or for a different amount. Keep your own ledger entry for each credit and never update a balance twice for the same event ID.", testing:"Send the listed Sandbox transfer amounts and verify successful, late and mismatched credits. Replay the same event to test deduplication." },
  { slug:"amount-mismatches", group:"Accept payments", title:"Amount mismatches", description:"Handle transfers that differ from the amount you requested.", endpointSlug:"create-bank-payment", purpose:"Bank customers sometimes send less or more than the displayed amount. Raha Pay records the actual credit so your application can apply a predictable policy instead of silently marking the original order paid.", useWhen:"Apply this guidance to bank transfers and one-time account numbers. Wallet prompts normally authorize the exact requested amount and do not use this flow.", build:"Store requested_amount and received_amount separately. Decide whether to hold, partially credit, request the balance or refund an overpayment. Show the decision in your support tools and ledger.", failures:"Do not alter the original bank credit. Do not infer the intended order from amount alone. Escalate credits that cannot be matched by account mapping and reference.", testing:"Force an underpayment and overpayment in Sandbox. Confirm that neither automatically fulfils an exact-price order unless your written business rule allows it." },
  { slug:"payouts-work", group:"Send money", title:"How payouts work", description:"Understand the payout lifecycle before sending money.", endpointSlug:"create-mobile-payout", diagram:"payout", statuses:["initiated","processing","successful","failed","reversed"], purpose:"A payout moves money from your available Raha Pay balance to a mobile wallet or Kenyan bank account. The create response confirms acceptance; the destination network determines the final result.", useWhen:"Use payouts for suppliers, refunds, marketplace sellers and customer withdrawals. Use a payment refund operation when you must return a specific card or wallet payment through its original rail.", build:"Validate the recipient, check your available balance, create the payout with a unique reference and store the returned ID. Keep the recipient view processing until a signed final event arrives.", failures:"Insufficient balance stops the payout before submission. A destination can reject incorrect details. A successful payout can later reverse. Reconcile every final status instead of treating processing as delivered.", testing:"Test success, insufficient balance, invalid recipient, network failure and reversal. Reuse an idempotency key only for an identical retry after an uncertain response." },
  { slug:"mobile-payouts", group:"Send money", title:"Payouts to M-Pesa and Airtel Money", description:"Send funds to a Kenyan mobile wallet.", endpointSlug:"create-mobile-payout", diagram:"payout", statuses:["initiated","processing","successful","failed","reversed"], purpose:"A mobile payout sends funds to an M-Pesa or Airtel Money wallet. Raha Pay validates the number and balance, submits the transfer, and reports the destination result asynchronously.", useWhen:"Use mobile payouts for recipients who identify by phone number. Use a bank payout when the recipient needs funds in a bank account or the amount exceeds wallet limits.", build:"Confirm the displayed recipient number, normalize it to 2547XXXXXXXX, set the correct network, and create the payout from your server. Save the rp_po_ ID and do not promise delivery while processing.", failures:"The number can belong to the wrong network, exceed account limits, be inactive or temporarily unreachable. A network acknowledgement can still reverse later.", testing:"Use the Sandbox numbers for success, insufficient balance, invalid recipient and reversal. Verify your ledger creates compensating entries for reversals." },
  { slug:"bank-payouts", group:"Send money", title:"Payouts to Kenyan bank accounts", description:"Verify a recipient and send funds to a Kenyan bank.", endpointSlug:"create-bank-payout", diagram:"payout", statuses:["initiated","processing","successful","failed","reversed"], purpose:"A bank payout credits an account identified by the current Raha Pay bank code and account number. An account lookup reduces mistakes by returning the bank's account name before you send.", useWhen:"Use a bank payout when the recipient gives an account number. Use a mobile payout for M-Pesa or Airtel Money. Never guess a bank code from a display name.", build:"Fetch banks, run account lookup, show the returned name for confirmation, then create the payout using that exact name. Store the ID, reference, beneficiary and amount in your ledger.", failures:"A valid-looking account may belong to another person. Banks can reject dormant accounts, limits or unsupported characters in narration. A completed transfer can reverse.", testing:"Exercise valid, not-found, name-mismatch, insufficient-balance and reversed outcomes. Verify operators can see the confirmed beneficiary before approval." },
  { slug:"account-lookup", group:"Send money", title:"Account name lookup", description:"Confirm a bank account name before creating a payout.", endpointSlug:"lookup-account", purpose:"Account lookup asks the selected bank for the name attached to an account number. It helps the sender catch typing mistakes before money leaves your balance.", useWhen:"Run lookup immediately before a bank payout. It is not proof of identity and should not replace your customer verification or fraud controls.", build:"Use the bank code from the banks endpoint and send the account number exactly as entered. Display the returned name and require the sender to confirm it before creating the payout.", failures:"Banks can be temporarily unavailable, an account can be closed, or the entered bank code can be wrong. Do not cache a successful lookup indefinitely.", testing:"Use the Sandbox valid and missing account numbers. Confirm that your payout action remains unavailable until a successful lookup is confirmed." },
  { slug:"balance", group:"Account", title:"Balance", description:"Read the funds available for payouts and reconciliation.", endpointSlug:"get-balance", purpose:"The balance endpoint separates funds available to send, funds still pending, and the ledger total. Use these values for operational visibility, not as a replacement for your own transaction ledger.", useWhen:"Read the balance before a payout batch and during reconciliation. Avoid polling it for every page render; cache briefly and refresh after relevant events.", build:"Call the endpoint from your server and display amounts as currency after converting integer cents. Record the read time so operators know how current the value is.", failures:"A pending movement can change after the read. A payout may still fail if another process reserves funds first. Handle insufficient_balance from the create call.", testing:"Add simulated Sandbox funds, create payouts and compare available, pending and ledger values after each final event." },
  { slug:"transactions", group:"Account", title:"Transactions", description:"List account activity and retrieve one canonical record.", endpointSlug:"list-transactions", purpose:"Transactions provide a consistent ledger view across payments, payouts and dedicated account credits. Use them to reconcile your database and investigate a known Raha Pay ID or merchant reference.", useWhen:"Use events for immediate product updates and transactions for periodic reconciliation or support searches. Avoid aggressive polling for final statuses.", build:"Request pages in a stable order, persist the cursor, and compare each item with your ledger. Retrieve one transaction when resolving a specific discrepancy.", failures:"New records can arrive while you paginate. De-duplicate by Raha Pay ID and use timestamps as ordering hints rather than unique keys.", testing:"Create several Sandbox movements with different statuses and references. Verify filters, pagination, duplicate protection and daily totals." },
  { slug:"webhooks-overview", group:"Events", title:"Webhooks overview", description:"Receive signed status updates at your HTTPS endpoint.", diagram:"webhook", purpose:"Webhooks tell your server when an asynchronous payment, payout or dedicated account credit changes state. Raha Pay signs the raw body and retries delivery when your endpoint does not acknowledge it.", useWhen:"Use webhooks for final business updates. Use a GET endpoint when a user actively requests a refresh or your reconciliation job needs to confirm stored state.", build:"Register a public HTTPS endpoint, read the raw request bytes, verify Raha-Signature, store the event ID with a unique constraint, queue the business update and return HTTP 2xx quickly.", failures:"Events can arrive more than once, later than expected or out of order. A 2xx response means your server accepted responsibility for the event, not that every downstream job finished.", testing:"Raha Pay retries five times over 24 hours with increasing delays. In Sandbox, return a failure deliberately, observe retries, then restore 200 handling and confirm one business update." },
  { slug:"verify-webhooks", group:"Events", title:"Verifying signatures", description:"Verify every event against the untouched request body.", diagram:"webhook", purpose:"Signature verification proves that the request body was signed with your webhook secret. Verification must happen before JSON parsing and before any transaction or order is changed.", useWhen:"Verify every webhook in both environments. Do not use IP addresses, a user-agent string or an unguessable URL as a substitute for cryptographic verification.", build:"Read Raha-Signature, calculate HMAC-SHA256 over the exact raw bytes, decode both digests to equal-length buffers, and compare them in constant time. Reject missing or invalid signatures with 401.", failures:"Common mistakes are parsing the body first, using normal equality instead of a constant-time comparison, comparing different encodings, or using the other environment's secret.", testing:"Replay a valid Sandbox payload, change one byte, remove the header and use a Live secret against a Sandbox event. Only the unchanged body with the correct secret should pass." },
  { slug:"event-catalogue", group:"Events", title:"Event catalogue", description:"See when every event fires and inspect its complete payload.", purpose:"The catalogue is the contract between asynchronous Raha Pay operations and your event handler. Subscribe to the events that drive a real business state change and safely acknowledge unknown future event types.", useWhen:"Use this page while designing handlers, tests and monitoring. Use the object-specific guide for the complete payment or payout flow.", build:"Route by type after verification and deduplication. Store the whole event for audit, but update your domain model only from the fields you explicitly support.", failures:"An event type can be delivered more than once and unrelated event types can interleave. Do not depend on delivery order or exhaustively reject a payload because it includes a new field.", testing:"Send every catalogue event from Sandbox and replay each one. Confirm unknown types return 200 without changing data." },
  { slug:"retries-idempotency", group:"Events", title:"Retries and idempotency", description:"Make create requests and event handlers safe to repeat.", purpose:"Distributed systems can lose a response even after work completed. Idempotency lets you repeat the same create request without creating a second payment or payout, while event deduplication prevents repeated delivery from applying an update twice.", useWhen:"Send an Idempotency-Key on every POST that creates money movement. Deduplicate every event by its rp_evt_ ID.", build:"Generate a key for one business attempt, store it with the request body, and reuse both after network uncertainty. Use a database uniqueness rule for event IDs before queueing work.", failures:"Reusing a key with a changed body returns idempotency_conflict. Creating a new key after every timeout can duplicate work. Retrying permanent validation errors wastes capacity.", testing:"Drop the client connection after sending a Sandbox create call, retry the identical call and confirm the same object ID returns. Replay an event and confirm one ledger update." },
  { slug:"statuses", group:"Reference", title:"Status codes and meanings", description:"Map payment and payout states to safe product actions.", purpose:"Statuses tell you whether an object is waiting, moving or final. Keep non-final records open for later events and ensure a final state cannot be overwritten by an older delivery.", useWhen:"Use this reference when mapping Raha Pay states into order, payout and ledger models.", build:"Store both the current status and its event timestamp. Apply only valid forward transitions, render processing states clearly and make final outcomes visible to support teams.", failures:"Do not treat initiated, awaiting_customer or processing as success. A payout can reverse after success, so model reversal as a compensating final outcome.", testing:"Feed transitions in order, duplicate them and send them out of order. The stored state should remain correct." },
  { slug:"errors", group:"Reference", title:"Errors", description:"Search error codes and choose a specific recovery action.", purpose:"Raha Pay errors use HTTP status for the broad class and data.code for the precise condition. Your application should log the request ID and code, then show a safe action rather than a raw provider message.", useWhen:"Use this catalogue in integration code, operations tooling and support runbooks.", build:"Separate validation failures, authentication failures, conflicts, temporary limits and server failures. Retry only temporary failures, with backoff and the same idempotency key.", failures:"Blind retries can duplicate intent, prolong an outage or lock out a customer. Never expose secret values, raw stack traces or sensitive account details in client logs.", testing:"Force representative 400, 401, 403, 404, 409, 422, 429 and 500 responses. Assert the customer action and operator log for each class." },
  { slug:"banks-networks", group:"Reference", title:"Banks and mobile networks", description:"Find supported Kenyan banks, bank codes and wallet identifiers.", purpose:"Destination identifiers are API values, not display labels. Fetch the active bank list when building account selection and send mpesa or airtel_money for mobile operations.", useWhen:"Use this reference for payout forms, account lookup and support tools. Refresh bank data periodically rather than embedding a permanent list in your application.", build:"Cache the banks endpoint briefly, retain the returned bank_code as a string, and display the official bank name. Validate mobile numbers against the selected network before submission.", failures:"Banks can be disabled temporarily and codes can contain leading zeroes. Do not convert codes to numbers or infer a network from a phone prefix alone.", testing:"Search the reference table, submit supported and unsupported codes, and confirm a disabled destination cannot be selected." },
  { slug:"limits-allowlisting", group:"Reference", title:"Rate limits and IP allowlisting", description:"Control Live access and recover safely when request volume is high.", purpose:"Rate limits protect account and network capacity. IP allowlisting restricts Live API keys to requests from public addresses you register in the dashboard.", useWhen:"Configure allowlisting before launch when your servers have stable egress addresses. Read rate-limit headers for batch jobs and high-volume traffic.", build:"Register every production egress address, monitor remaining capacity, queue bursts and apply exponential backoff with jitter after 429 responses. Honour Retry-After when present.", failures:"Autoscaling or third-party workers may use addresses you did not register. Retrying all workers at the same moment creates another spike. Webhook delivery is not restricted by your outbound API allowlist.", testing:"Call Live readiness checks from every production region, and load-test Sandbox below and above the documented window." },
  { slug:"changelog", group:"Reference", title:"Changelog", description:"Track API and documentation changes that may affect your integration.", purpose:"The changelog records additions, behaviour clarifications, deprecations and migration deadlines. Breaking API changes use a new version and a migration notice.", useWhen:"Review this page during dependency updates and before each planned release.", build:"Subscribe your engineering owner to release notices, record the API version you use and schedule deprecated behaviour before its removal date.", failures:"Ignoring a deprecation notice can leave a working integration dependent on retired behaviour. Treat webhook additions as forward-compatible and ignore fields you do not use.", testing:"Run your Sandbox regression suite after each relevant entry and before promoting the same release to Live." }
];

const defaultStatuses = ["initiated", "processing", "successful", "failed"];
const defaultTests = [["254712345678", "successful"], ["254712345679", "insufficient funds"], ["254712345680", "customer cancelled"], ["254712345681", "timeout"]];

function buildSections(item: typeof guidesData[number]): GuideSection[] {
  if (item.sections) return item.sections;
  const sections: GuideSection[] = [
    { id:"purpose", title:"What this is for", paragraphs:[item.purpose, item.useWhen] },
  ];
  if (item.diagram) sections.push({ id:"flow", title:"How the flow works", paragraphs:["Follow the sequence from your server to the payment rail. The signed event closes the asynchronous part of the flow."], diagram:item.diagram });
  sections.push(
    { id:"build", title:"How to build it", paragraphs:[item.build, "Keep credentials on your server, store Raha Pay identifiers with your own references, and make each state change safe to apply more than once."] },
    { id:"problems", title:"What can go wrong", paragraphs:[item.failures], callout:{ kind:"warning", title:"Act on final state", text:"A transport timeout or accepted request is not proof of the final money movement. Confirm the stored status before taking irreversible action." } },
    { id:"testing", title:"Test it in Sandbox", paragraphs:[item.testing] },
  );
  if (["quickstart","payments-work","payouts-work"].includes(item.slug)) sections.push({ id:"go-live", title:"Go live checklist", bullets:["Switch to Live API keys and the Live base URL.","Register the Live webhook URL and secret.","Verify Raha-Signature before parsing each event.","Handle every final status without duplicate fulfilment.","Reconcile your ledger against transactions every day."] });
  return sections;
}

export const guides: Guide[] = guidesData.map((item) => {
  const transactional = ["Accept payments","Send money","Events"].includes(item.group) || item.quickstart;
  const steps: GuideStep[] | undefined = transactional ? [
    { id:"step-1", title:"Prepare the operation", paragraphs:[item.useWhen, "Validate the customer or recipient details before sending anything to Raha Pay."], codeKind:"request" },
    { id:"step-2", title:"Create and store it", paragraphs:[item.build, "Persist the returned ID and your own reference in the same transaction where possible."], codeKind:"response" },
    { id:"step-3", title:"Process the final result", paragraphs:[item.failures, "Verify and deduplicate the event before updating your order or ledger."], codeKind:"webhook" },
  ] : undefined;
  if (item.quickstart && steps) {
    steps.splice(0, steps.length,
      { id:"step-1", title:"Get your Sandbox key", paragraphs:["Open the Raha Pay dashboard, create a Sandbox secret key and store it in your server environment."], outcome:"You should see a key beginning with sk_test_.", codeKind:"request" },
      { id:"step-2", title:"Register your webhook", paragraphs:["Add a public HTTPS endpoint and copy the Sandbox signing secret into your secret store."], outcome:"You should see the endpoint marked active in the dashboard.", codeKind:"webhook" },
      { id:"step-3", title:"Send the STK push", paragraphs:[item.build], outcome:"You should see an rp_pay_ ID with awaiting_customer status.", codeKind:"request" },
      { id:"step-4", title:"Receive the signed event", paragraphs:[item.failures], outcome:"You should see payment.successful for the success test number.", codeKind:"webhook" },
      { id:"step-5", title:"Confirm the stored payment", paragraphs:["Retrieve the payment by ID and compare its amount, reference and final status with your order."], outcome:"You should see successful and KES 12,500.00.", codeKind:"response" },
    );
  }
  return { ...item, sections:buildSections(item), steps, statuses:item.statuses ?? (transactional ? defaultStatuses : undefined), edgeCases: transactional ? ["Use one merchant reference for one business operation.","Keep secret keys out of browsers, mobile applications and logs.","Treat network timeouts as unknown until you retrieve the object.","Deduplicate webhook deliveries by event ID.","Ignore older state updates after a final status."] : undefined, sandboxTests: transactional ? defaultTests : undefined };
});

export const guideGroups = ["Start here","Accept payments","Send money","Account","Events","Reference"];
export const getGuide = (slug:string) => guides.find((item) => item.slug === slug);
